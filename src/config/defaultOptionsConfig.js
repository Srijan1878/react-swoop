const defaultOptionsConfig = {
  loop: false,
  auto: false,
  animationType: 'slide',
  speed: 0.5,
  showTabs: true,
  interval: 2000,
  draggable: false,
  direction: 'horizontal'
}

export default defaultOptionsConfig
